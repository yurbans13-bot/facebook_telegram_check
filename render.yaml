services:
  - type: worker
    name: facebook_telegram_check
    env: python
    plan: starter  # Или free, если на бесплатном плане
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python main.py
