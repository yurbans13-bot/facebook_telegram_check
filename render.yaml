services:
  - type: worker
    name: facebook_telegram_check
    env: python
    plan: starter  # или free, если используешь бесплатный план
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
